powershell -Command "$content = Get-Content requirements.txt; $new_content = $content | ForEach-Object { $pkg_name = ($_ -split '[<>=!~]')[0].Trim(); if (-not [string]::IsNullOrWhiteSpace($pkg_name) -and -not $_.StartsWith('#')) { $ver_line = pip show $pkg_name | Select-String 'Version:'; if ($ver_line) { $ver = $ver_line.ToString().Split(':')[1].Trim(); \"$pkg_name==$ver\" } else { $_ } } else { $_ } }; $new_content | Set-Content requirements.txt"